export const INVALID_USER = {
    EXCEEDS_CHARACTER : 'Field exceeds allowed characters.',
    FIELD_REQUIRED: (field: string) => (`${field} is required.`)
}


export const LENGTH_TEXT = {
    MAX_LENGTH_FULL_NAME: 25
}